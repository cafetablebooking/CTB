export enum ThemeMode {
  LIGHT = 'light',
  DARK = 'dark',
}

export enum HeaderType {
  DARK = 'dark',
  LIGHT = 'light',
}

export enum Fonts {
  LIGHT = 300,
  REGULAR = 400,
  MEDIUM = 500,
  BOLD = 600,
  EXTRA_BOLD = 700,
}

export enum AuthType {
  FIREBASE = 'firebase',
}
